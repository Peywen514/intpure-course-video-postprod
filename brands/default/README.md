# brands/default 品牌素材

**目前 `intro.mp4` / `outro.mp4` 是測試用的佔位檔**（直接從 `input/1-1-未上字幕.mp4` 剪下來的片段，
只是用來驗證 `06_bumper_concat.py` 的 concat 流程能不能跑通），**不是正式的品牌片頭尾**。

等拿到實際的 LOGO / 指定用色 / 既有片頭影片後，直接覆蓋這兩個檔案即可，
`06_bumper_concat.py` 的呼叫方式不需要改。
